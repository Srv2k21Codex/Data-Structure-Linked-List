#include<iostream>
using namespace std;

int prod(int a, int b)
{
    return a*b; 
}

int main(){
    
    int a,b;
    cout<<"enter the value of a and b"<<endl;
    cin>>a>>b;
    cout<<"product  will be"<<prod(a,b);
    cout<<"product  will be"<<prod(a,b);
    cout<<"product  will be"<<prod(a,b);
    cout<<"product  will be"<<prod(a,b);
    cout<<"product  will be"<<prod(a,b);
    cout<<"product  will be"<<prod(a,b);
    cout<<"product  will be"<<prod(a,b);
    cout<<"product  will be"<<prod(a,b);
    cout<<"product  will be"<<prod(a,b);
    
//cout<<"product  will be"<<prod(a,b);
//    
//cout<<"product  will be"<<prod(a,b);
//    
//cout<<"product  will be"<<prod(a,b);
//    
//cout<<"product  will be"<<prod(a,b);
    
    return 0;
}